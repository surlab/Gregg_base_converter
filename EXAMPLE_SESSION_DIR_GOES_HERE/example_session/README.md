# Raw session inputs

Place scattered acquisition files for one session here (suite2p outputs, behavior CSVs, imaging XML, voltage TTL, etc.).

The converter discovers files under this tree. See `example_past_converter/EXAMPLE_SESSION_DIR_GOES_HERE/example_session/` in the archived Tricolor plan for a typical layout (binaries not included in this template).
