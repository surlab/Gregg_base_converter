# SurLab export sentinels (Tricolor)

Small example files from a real export. Full `.npz` arrays are not included.

- `GCaMP8m_traces_schema.json` — stream schema
- `GCaMP8m_traces_metadata.csv` — first few ROI rows
- `dataset_config.json`, `sessionInfo_tricolor_projections.csv` — dataset-level metadata
