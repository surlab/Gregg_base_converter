# Reference access / preprocessing code

Place reference scripts from the source lab here (`.py`, `.m`, etc.) — read-only context for the converter, not executed by the template.

See `example_past_converter/EXAMPLE_ACCESS_CODE_GOES_HERE/` for a Tricolor preprocessing example.
